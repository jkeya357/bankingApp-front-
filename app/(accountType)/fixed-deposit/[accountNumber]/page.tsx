import AccountProp from "@/components/Account-Components/AccountProp";

const page = () => {
  return (
    <div>
      <AccountProp accountType="FIXED_DEPOSIT" account={"Fixed deposit"} />
    </div>
  );
};

export default page;
